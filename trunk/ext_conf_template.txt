# cat=basic; type=text; label=Standard URL:The URL of your non optimised website
standard_url = www.domain.com
    
# cat=basic; type=text; label=Mobile URL:This is the URL the extension redirects to   
mobile_url = mobile.domain.com

# cat=basic; type=boolean; label=Use Cookie?
use_cookie = 1

# cat=basic; type=text; label=Cookie name:The name under which the Cookie is stored
cookie_name = tx_mobileredirect

# cat=basic; type=text; label=Cookie lifetime:Seconds until the Cookie expires
cookie_lifetime = 3600

# cat=basic; type=text; label=is mobile GET name:GET-Parameter to force mobile version (e.g. www.domain.com?isMobile)
is_mobile_name = isMobile

# cat=basic; type=text; label=no mobile GET name:GET-Parameter to force mobile version (e.g. www.domain.com?noMobile)  
no_mobile_name = noMobile

# cat=basic; type=boolean; label=Adds "=1" to GET-Parameters:If enabled, the GET-Parameter is used with "=1" added (e.g. ?isMobile=1) to solve problems with RealURL and pageHandling
add_value_to_params = 1

# cat=basic; type=boolean; label=Enable mobile detection:Enables the automatic mobile detection. If disabled, only the GET-Parameters will work.  
detection_enabled = 1

# cat=basic; type=boolean; label=Enable redirection to mobile version:Enable redirection to mobile version.  
redirection_enabled = 1
                                                                                                                                                                         

























                                          